# 商品模块使用指南

## 📦 模块概述

商品模块已完成开发，包含完整的商品管理、分类管理、评价系统和收藏功能。严格参考社区模块的代码模式实现。

## 📋 已完成的功能

### 后端功能
✅ **6个数据库表**
- `product_category` - 商品分类表（两层分类）
- `product` - 商品主表
- `product_sku` - 商品规格表（简单规格）
- `product_favorite` - 商品收藏表
- `product_review` - 商品评价表
- `product_review_like` - 评价点赞表

✅ **3个服务层**
- `ProductService` - 商品核心业务
- `ProductCategoryService` - 分类管理
- `ProductReviewService` - 评价管理

✅ **6个控制器**
- 管理后台：商品管理、分类管理、评价管理
- 用户端：商品浏览、收藏功能、评价功能

### 前端功能
✅ 商品列表页面（已对接）
✅ API接口文件更新

---

## 🚀 部署步骤

### 第一步：创建数据库表

1. 找到SQL文件：`cool-admin-midway/database/product_module.sql`

2. 执行SQL脚本（两种方式）：

**方式A：使用数据库客户端（推荐）**
```bash
# 打开 Navicat / DBeaver / MySQL Workbench
# 连接到数据库
# 打开 product_module.sql 文件
# 执行整个脚本
```

**方式B：使用命令行**
```bash
# 进入项目目录
cd D:\wudong_project\code\cool-admin-midway

# 执行SQL（请替换为实际的数据库信息）
mysql -u root -p your_database_name < database/product_module.sql
```

3. 验证表是否创建成功
```sql
-- 查看所有商品相关表
SHOW TABLES LIKE 'product%';

-- 应该看到6张表：
-- product
-- product_category
-- product_sku
-- product_favorite
-- product_review
-- product_review_like

-- 查看测试数据
SELECT * FROM product_category;  -- 应该有4个一级分类、12个二级分类
SELECT * FROM product;           -- 应该有8个测试商品
```

---

### 第二步：启动后端服务

```bash
# 进入后端项目目录
cd D:\wudong_project\code\cool-admin-midway

# 安装依赖（如果还没安装）
npm install
# 或
pnpm install

# 启动开发服务器
npm run dev
# 或
pnpm dev
```

启动成功后，应该看到：
```
✔ Your server is ready to go!
📚 API服务地址: http://127.0.0.1:7001
```

---

### 第三步：启动前端服务

```bash
# 进入前端项目目录
cd D:\wudong_project\code\wudong-web

# 安装依赖（如果还没安装）
npm install
# 或
pnpm install

# 启动开发服务器
npm run dev
# 或
pnpm dev
```

启动成功后，访问：`http://localhost:5173`

---

## 🧪 测试验证

### 1. 测试商品列表接口

**打开浏览器控制台，在商品列表页面查看网络请求：**

```
请求：POST /app/product/productList
响应：
{
  "code": 1000,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "苗族手工银饰手镯",
        "mainImage": "...",
        "price": 298.00,
        "sales": 156,
        ...
      }
    ],
    "pagination": {
      "page": 1,
      "size": 10,
      "total": 8
    }
  }
}
```

### 2. 测试商品详情接口

**访问商品详情页（点击任意商品）：**

```
请求：GET /app/product/productDetail?id=1
响应：
{
  "code": 1000,
  "data": {
    "id": 1,
    "title": "苗族手工银饰手镯",
    "detail": "<p>商品详情...</p>",
    "skus": [],
    "isFavorited": false,
    ...
  }
}
```

### 3. 测试分类接口

**在浏览器控制台输入：**
```javascript
fetch('http://localhost:7001/app/product/categoryTree')
  .then(r => r.json())
  .then(console.log)

// 应该返回分类树结构
```

### 4. 测试收藏功能（需要登录）

如果没有登录功能，测试用户ID会默认为1（代码中已处理）。

---

## 📝 接口文档

### 用户端接口

| 接口 | 方法 | 路径 | 说明 | 是否需要登录 |
|------|-----|------|------|-------------|
| 商品列表 | POST | `/app/product/productList` | 分页、筛选、排序 | ❌ |
| 商品详情 | GET | `/app/product/productDetail` | 包含SKU列表 | ❌ |
| 分类树 | GET | `/app/product/categoryTree` | 两层分类树 | ❌ |
| 收藏商品 | POST | `/app/product/favoriteProduct` | - | ✅ |
| 取消收藏 | POST | `/app/product/unfavoriteProduct` | - | ✅ |
| 我的收藏 | POST | `/app/product/myFavorites` | 分页 | ✅ |
| 评价列表 | POST | `/app/product/review/reviewList` | 分页 | ❌ |
| 发布评价 | POST | `/app/product/review/publishReview` | - | ✅ |
| 点赞评价 | POST | `/app/product/review/likeReview` | - | ✅ |
| 取消点赞 | POST | `/app/product/review/unlikeReview` | - | ✅ |

### 管理后台接口

| 模块 | 自动接口 | 说明 |
|------|---------|------|
| 商品管理 | add/delete/update/info/list/page | Cool-Admin自动生成 |
| 分类管理 | add/delete/update/info/list/page | Cool-Admin自动生成 |
| 评价管理 | add/delete/update/info/list/page | Cool-Admin自动生成 |

**额外接口：**
- `POST /admin/product/updateStatus` - 上架/下架商品
- `POST /admin/product/setRecommend` - 设置推荐
- `POST /admin/product/review/updateStatus` - 审核评价
- `POST /admin/product/review/replyReview` - 商家回复

---

## 🎯 使用Postman测试

### 1. 获取商品列表
```
POST http://localhost:7001/app/product/productList
Content-Type: application/json

{
  "page": 1,
  "size": 10,
  "sort": "sales"
}
```

### 2. 获取商品详情
```
GET http://localhost:7001/app/product/productDetail?id=1
```

### 3. 收藏商品
```
POST http://localhost:7001/app/product/favoriteProduct
Content-Type: application/json

{
  "productId": 1
}
```

### 4. 发布评价
```
POST http://localhost:7001/app/product/review/publishReview
Content-Type: application/json

{
  "productId": 1,
  "rating": 5,
  "content": "商品非常好，质量很棒！",
  "images": ["https://example.com/image1.jpg"]
}
```

---

## 🔧 常见问题

### Q1: 数据库表创建后，启动报错找不到表？

**A:** 检查实体类的表名是否和数据库一致
```typescript
// 实体类中
@Entity('product')  // 确保表名正确
export class ProductEntity extends BaseEntity {
```

### Q2: 接口返回404？

**A:** 检查几点：
1. 模块是否正确放置在 `src/modules/product/` 目录
2. 是否有 `config.ts` 文件
3. 重启后端服务

### Q3: 接口返回空数据？

**A:** 确认：
1. SQL脚本是否正确执行
2. 数据库连接配置是否正确
3. 查询条件是否正确（如：status = 1）

### Q4: 收藏/评价功能报"用户未登录"？

**A:** 代码中已设置默认测试用户ID为1，如果还报错：
1. 检查是否有JWT认证中间件拦截
2. 临时关闭认证测试：在控制器上加 `@CoolTag(TagTypes.IGNORE_TOKEN)`

### Q5: 前端页面加载不出商品？

**A:** 打开浏览器控制台（F12）检查：
1. 网络请求是否正常发送
2. 接口返回的数据格式是否正确
3. 控制台是否有JavaScript错误

---

## 📂 项目结构

```
cool-admin-midway/src/modules/product/
├── config.ts                           # 模块配置
├── entity/                             # 实体层（6个文件）
│   ├── category.ts                     # 分类实体
│   ├── product.ts                      # 商品实体
│   ├── sku.ts                          # SKU实体
│   ├── favorite.ts                     # 收藏实体
│   ├── review.ts                       # 评价实体
│   └── review_like.ts                  # 评价点赞实体
├── service/                            # 服务层（3个文件）
│   ├── category.ts                     # 分类服务
│   ├── product.ts                      # 商品服务
│   └── review.ts                       # 评价服务
└── controller/                         # 控制器层
    ├── admin/                          # 管理后台（3个文件）
    │   ├── category.ts                 # 分类管理
    │   ├── product.ts                  # 商品管理
    │   └── review.ts                   # 评价管理
    └── app/                            # 用户端（2个文件）
        ├── product.ts                  # 商品浏览
        └── review.ts                   # 评价功能

wudong-web/src/api/
└── product.ts                          # 前端API接口（已更新）

wudong-web/src/views/product/
├── List.vue                            # 商品列表页（已更新）
├── Detail.vue                          # 商品详情页
└── Cart.vue                            # 购物车页
```

---

## 🎉 下一步

商品模块已完成，你可以：

1. **测试所有功能** - 按照上面的测试步骤验证
2. **开发商品详情页** - 完善 `wudong-web/src/views/product/Detail.vue`
3. **继续开发其他模块**：
   - 餐饮模块（food）
   - 住宿模块（accommodation）
   - 票务模块（travel）
   - 订单模块（order）
   - 购物车模块（cart）

每个模块都将参考这个商品模块和社区模块的实现模式！

---

## 📞 需要帮助？

如果遇到问题：
1. 检查控制台错误日志
2. 查看数据库连接是否正常
3. 验证接口返回的数据格式
4. 对比社区模块的实现方式

祝开发顺利！🚀
